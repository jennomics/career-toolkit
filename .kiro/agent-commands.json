{
  "commands": [
    {
      "id": "kill-old-dev-4",
      "command": "kill 41544; sleep 2; kill $(lsof -t -i:3001) 2>/dev/null; sleep 1; rm -rf .next && npm run dev",
      "description": "Kill stale dev server on port 3000, clear cache, restart fresh"
    }
  ],
  "createdAt": "2026-07-28T00:16:00Z",
  "source": "kiro"
}
