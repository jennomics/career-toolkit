{
  "commands": [
    {
      "id": "llm-1",
      "command": "npm install && npx prisma generate && npx prisma db push && mv dev.db prisma/dev.db 2>/dev/null; true",
      "description": "Install openai package, regenerate Prisma, sync database"
    }
  ],
  "createdAt": "2026-07-26T23:15:00Z",
  "source": "kiro"
}
