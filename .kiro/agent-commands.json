{
  "commands": [
    {
      "id": "env-1",
      "command": "cat .env | grep OPENAI_API_KEY || echo 'MISSING: OPENAI_API_KEY not found in .env'",
      "description": "Check if OPENAI_API_KEY is in .env"
    }
  ],
  "createdAt": "2026-07-26T23:40:00Z",
  "source": "kiro"
}
