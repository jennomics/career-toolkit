{
  "commands": [
    {
      "id": "del-fix-1",
      "command": "npx prisma generate && npx prisma db push && mv dev.db prisma/dev.db 2>/dev/null; true",
      "description": "Sync database schema (ensure JobResponsibility table exists)"
    }
  ],
  "createdAt": "2026-07-26T23:00:00Z",
  "source": "kiro"
}
